apiVersion: v1
kind: Service
metadata:
  name: vibecode-service
  namespace: default
  labels:
    app: tiktok-vibecode-backend
    service: vibecode
spec:
  selector:
    app: tiktok-vibecode-backend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    name: http
  type: ClusterIP

